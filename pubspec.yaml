name: web_game_engine
description: 2.5d web game engine for flutter
version: 0.0.2
repository: https://github.com/BlackJet1/web_game_engine

environment:
  sdk: ^3.5.3
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_gl: ^0.0.21
  vector_math: ^2.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
flutter: